version: '3.8'

services:
  bot:
    build: .
    container_name: tg_transcribe_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot_data:/app
      # Опционально: можно вынести базу данных
      # - ./bot.db:/app/bot.db

volumes:
  bot_data:
